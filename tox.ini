[tox]
skipsdist = true
envlist = py35, openapi

[testenv]
usedevelop = true
deps =
    pytest
    pytest-cov
    flake8
commands =
    python setup.py check --strict
    flake8 xsnippet_api/ tests/
    py.test tests/ --cov --cov-append

[testenv:openapi]
usedevelop = false
deps = flex
commands =
    swagger-flex -s contrib/openapi/spec.yml
